//
//  AdaptyAttributionSource.swift
//  IntegrationKit — Checks/Stubs
//
//  Stand-in for the attribution-source enum `Adapty.updateAttribution(source:)` takes. Only the
//  two networks `AdaptyService` actually names.
//

import Foundation

public enum AdaptyAttributionSource {
	case adjust
	case appsflyer
}
